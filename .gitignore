# Python bytecode
__pycache__/
*.py[cod]

# Environment / secrets
.env

# SQLite registry databases
*.db
*.db-journal

# Tooling caches
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# Virtual environment (also self-ignored via its own nested .gitignore,
# listed here explicitly so a recreated venv is covered either way)
.venv/
venv/

# Per-version skill code the registry regenerates; the DB is the source of truth.
skills/versioned_skills/
